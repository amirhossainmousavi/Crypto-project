import React from 'react';
import LoadingGif from '../gif/Loading.gif'

const Loading = () => {
    return (
        <div>
            <img src={LoadingGif}  />
        </div>
    );
};

export default Loading;